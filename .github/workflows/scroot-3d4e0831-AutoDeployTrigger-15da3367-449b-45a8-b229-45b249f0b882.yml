name: Trigger auto deployment for scroot-3d4e0831

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ master ]
    paths:
    - '**'
    - '.github/workflows/scroot-3d4e0831-AutoDeployTrigger-15da3367-449b-45a8-b229-45b249f0b882.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.SCROOT3D4E0831_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.SCROOT3D4E0831_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.SCROOT3D4E0831_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          registryUrl: cappsinttestregistryprivate.azurecr.io
          registryUsername: ${{ secrets.SCROOT3D4E0831_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.SCROOT3D4E0831_REGISTRY_PASSWORD }}
          containerAppName: scroot-3d4e0831
          resourceGroup: cappsint-eu2euap-cni-dedicated
          imageToBuild: cappsinttestregistryprivate.azurecr.io/scroot-3d4e0831:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


