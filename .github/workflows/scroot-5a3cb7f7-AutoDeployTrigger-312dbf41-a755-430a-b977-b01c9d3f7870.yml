name: Trigger auto deployment for scroot-5a3cb7f7

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ master ]
    paths:
    - '**'
    - '.github/workflows/scroot-5a3cb7f7-AutoDeployTrigger-312dbf41-a755-430a-b977-b01c9d3f7870.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.SCROOT5A3CB7F7_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.SCROOT5A3CB7F7_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.SCROOT5A3CB7F7_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          registryUrl: cappsinttestregistryprivate.azurecr.io
          registryUsername: ${{ secrets.SCROOT5A3CB7F7_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.SCROOT5A3CB7F7_REGISTRY_PASSWORD }}
          containerAppName: scroot-5a3cb7f7
          resourceGroup: cappsint-eastus-cni-nonvnet
          imageToBuild: cappsinttestregistryprivate.azurecr.io/scroot-5a3cb7f7:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


