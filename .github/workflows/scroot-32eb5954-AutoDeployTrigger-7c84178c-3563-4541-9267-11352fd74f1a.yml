name: Trigger auto deployment for scroot-32eb5954

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ master ]
    paths:
    - '**'
    - '.github/workflows/scroot-32eb5954-AutoDeployTrigger-7c84178c-3563-4541-9267-11352fd74f1a.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.SCROOT32EB5954_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.SCROOT32EB5954_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.SCROOT32EB5954_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          registryUrl: cappsinttestregistryprivate.azurecr.io
          registryUsername: ${{ secrets.SCROOT32EB5954_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.SCROOT32EB5954_REGISTRY_PASSWORD }}
          containerAppName: scroot-32eb5954
          resourceGroup: cappsint-eastasia-cni-nonvnet
          imageToBuild: cappsinttestregistryprivate.azurecr.io/scroot-32eb5954:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


