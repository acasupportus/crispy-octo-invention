name: Trigger auto deployment for scroot-372d4d22

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ master ]
    paths:
    - '**'
    - '.github/workflows/scroot-372d4d22-AutoDeployTrigger-8eafa6e1-36df-4603-b19c-d8c0ab177a32.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.SCROOT372D4D22_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.SCROOT372D4D22_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.SCROOT372D4D22_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          registryUrl: cappsinttestregistryprivate.azurecr.io
          registryUsername: ${{ secrets.SCROOT372D4D22_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.SCROOT372D4D22_REGISTRY_PASSWORD }}
          containerAppName: scroot-372d4d22
          resourceGroup: cappsint-westus2-rg
          imageToBuild: cappsinttestregistryprivate.azurecr.io/scroot-372d4d22:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


