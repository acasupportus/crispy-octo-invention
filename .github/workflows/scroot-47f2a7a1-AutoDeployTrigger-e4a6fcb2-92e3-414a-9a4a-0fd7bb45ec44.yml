name: Trigger auto deployment for scroot-47f2a7a1

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ master ]
    paths:
    - '**'
    - '.github/workflows/scroot-47f2a7a1-AutoDeployTrigger-e4a6fcb2-92e3-414a-9a4a-0fd7bb45ec44.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.SCROOT47F2A7A1_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.SCROOT47F2A7A1_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.SCROOT47F2A7A1_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          registryUrl: cappsinttestregistryprivate.azurecr.io
          registryUsername: ${{ secrets.SCROOT47F2A7A1_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.SCROOT47F2A7A1_REGISTRY_PASSWORD }}
          containerAppName: scroot-47f2a7a1
          resourceGroup: cappsint-stage-cni-dedicated
          imageToBuild: cappsinttestregistryprivate.azurecr.io/scroot-47f2a7a1:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


