name: Trigger auto deployment for scroot-8853cc3f

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ master ]
    paths:
    - '**'
    - '.github/workflows/scroot-8853cc3f-AutoDeployTrigger-63f429ed-8f62-46c3-9c9a-f9f06c031ab9.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.SCROOT8853CC3F_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.SCROOT8853CC3F_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.SCROOT8853CC3F_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          registryUrl: cappsinttestregistryprivate.azurecr.io
          registryUsername: ${{ secrets.SCROOT8853CC3F_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.SCROOT8853CC3F_REGISTRY_PASSWORD }}
          containerAppName: scroot-8853cc3f
          resourceGroup: cappsint-cuseuap-rg
          imageToBuild: cappsinttestregistryprivate.azurecr.io/scroot-8853cc3f:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


