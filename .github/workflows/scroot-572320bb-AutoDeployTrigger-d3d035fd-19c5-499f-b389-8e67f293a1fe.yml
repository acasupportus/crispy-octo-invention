name: Trigger auto deployment for scroot-572320bb

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ master ]
    paths:
    - '**'
    - '.github/workflows/scroot-572320bb-AutoDeployTrigger-d3d035fd-19c5-499f-b389-8e67f293a1fe.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.SCROOT572320BB_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.SCROOT572320BB_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.SCROOT572320BB_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          registryUrl: cappsinttestregistryprivate.azurecr.io
          registryUsername: ${{ secrets.SCROOT572320BB_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.SCROOT572320BB_REGISTRY_PASSWORD }}
          containerAppName: scroot-572320bb
          resourceGroup: cappsint-stage-cni-dedicated-nonvnet
          imageToBuild: cappsinttestregistryprivate.azurecr.io/scroot-572320bb:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


