name: Trigger auto deployment for scroot-46b27a35

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ master ]
    paths:
    - '**'
    - '.github/workflows/scroot-46b27a35-AutoDeployTrigger-0040c2cd-518c-45e3-a0cf-c81a1a485c4a.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.SCROOT46B27A35_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.SCROOT46B27A35_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.SCROOT46B27A35_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          registryUrl: cappsinttestregistryprivate.azurecr.io
          registryUsername: ${{ secrets.SCROOT46B27A35_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.SCROOT46B27A35_REGISTRY_PASSWORD }}
          containerAppName: scroot-46b27a35
          resourceGroup: cappsint-rg
          imageToBuild: cappsinttestregistryprivate.azurecr.io/scroot-46b27a35:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


