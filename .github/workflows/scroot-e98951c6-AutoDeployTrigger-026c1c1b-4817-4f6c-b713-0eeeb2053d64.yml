name: Trigger auto deployment for scroot-e98951c6

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ master ]
    paths:
    - '**'
    - '.github/workflows/scroot-e98951c6-AutoDeployTrigger-026c1c1b-4817-4f6c-b713-0eeeb2053d64.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.SCROOTE98951C6_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.SCROOTE98951C6_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.SCROOTE98951C6_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          registryUrl: cappsinttestregistryprivate.azurecr.io
          registryUsername: ${{ secrets.SCROOTE98951C6_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.SCROOTE98951C6_REGISTRY_PASSWORD }}
          containerAppName: scroot-e98951c6
          resourceGroup: cappsint-eastasia-cni-nonvnet
          imageToBuild: cappsinttestregistryprivate.azurecr.io/scroot-e98951c6:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


